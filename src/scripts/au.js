import { burgerMenu, typingEffectVariousLines } from './main.js';

// Burger menu
burgerMenu();

// Typing effect
const typing = document.querySelectorAll('.about-us__title--highlight');
typingEffectVariousLines(typing);